# 🍔 Tasty Bites — Food Ordering Website

A simple food ordering demo website built with **HTML, CSS, and JavaScript**.
Created as part of my internship learning project.

## ✨ Features
- Browse a menu of food items
- Add items to a cart and adjust quantities (+ / −)
- See a live order total
- Place an order (demo confirmation)

## 🛠️ Built with
- **HTML** — page structure (`index.html`)
- **CSS** — styling and layout (`styles.css`)
- **JavaScript** — cart logic and interactivity (`script.js`)

## ▶️ Run it locally
You can open `index.html` directly, or serve it with Node:

```bash
npx serve . -l 3000
```

Then visit **http://localhost:3000**

## 📌 Note
This is a **front-end only** demo — there is no database or backend yet,
so orders are not saved. Cart contents reset on page refresh.

## 🚀 Next steps
- Deploy live on Vercel
- Add a backend + database to save real orders
